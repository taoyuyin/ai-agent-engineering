# Multi-Agent Runtime MVP

使用 capability card、最小 scope delegation、委派预算和 evidence contract 管理协作。

```bash
python chapters/chapter22/example.py
python -m unittest discover -s chapters/chapter22 -p "test_*.py"
```
